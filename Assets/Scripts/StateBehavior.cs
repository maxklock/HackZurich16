using UnityEngine;
using System.Collections;

public class StateBehavior : MonoBehaviour
{
    public StateBehavior NextBehavior;

    public bool CanChangeState { get; set; }

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
